# Optional in local dev.
# If omitted, auth/user data will use local fallback store:
# ./.local/auth-store.json
DATABASE_URL=postgres://user:password@host/db?sslmode=require

# Required in production for OAuth callback URL generation
APP_BASE_URL=https://your-domain.com

# Optional for local callback URL consistency
# APP_BASE_URL=http://localhost:3000

# Cloudflare deployment (for CI/local deploy commands)
# Prefer GitHub Secrets or your shell profile for real values.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=

# Google OAuth (fully wired)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional email verification delivery (Resend)
RESEND_API_KEY=
EMAIL_FROM=

# Optional custom OAuth providers (activate when all vars are set)
# OpenAI
OPENAI_OAUTH_CLIENT_ID=
OPENAI_OAUTH_CLIENT_SECRET=
OPENAI_OAUTH_AUTH_URL=
OPENAI_OAUTH_TOKEN_URL=
OPENAI_OAUTH_USERINFO_URL=
OPENAI_OAUTH_SCOPE=openid email profile

# Claude
CLAUDE_OAUTH_CLIENT_ID=
CLAUDE_OAUTH_CLIENT_SECRET=
CLAUDE_OAUTH_AUTH_URL=
CLAUDE_OAUTH_TOKEN_URL=
CLAUDE_OAUTH_USERINFO_URL=
CLAUDE_OAUTH_SCOPE=openid email profile

# Grok
GROK_OAUTH_CLIENT_ID=
GROK_OAUTH_CLIENT_SECRET=
GROK_OAUTH_AUTH_URL=
GROK_OAUTH_TOKEN_URL=
GROK_OAUTH_USERINFO_URL=
GROK_OAUTH_SCOPE=openid email profile
