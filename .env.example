# Clerk Authentication (required)
# Get these from https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Clerk redirect URLs
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/login
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/signup
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/practice
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/practice

# Database (optional in local dev — omit to use in-memory fallback)
# If omitted, knowledge state is stored in memory only (resets on restart)
DATABASE_URL=postgres://user:password@host/db?sslmode=require

# Cloudflare deployment (for CI/local deploy commands)
# Prefer GitHub Secrets or your shell profile for real values.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
